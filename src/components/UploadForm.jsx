const UploadForm = (props) => {
  return (
    <div>
      <p>This is a test...</p>
      <button type="button" onClick={props.onModalClose}>
        Close Modal
      </button>
    </div>
  );
};
export default UploadForm;
